import React from "react";
import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Competition_Content = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <View>
        <Text>Hello</Text>
      </View>
    </SafeAreaView>
  );
};

export default Competition_Content;
